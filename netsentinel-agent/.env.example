# Port this agent listens on (default: 9100)
AGENT_PORT=9101

# JWT secret — must match JWT_SECRET in the server's .env
# Generate with: openssl rand -hex 32
JWT_SECRET=change_me_to_a_long_random_hex_string
